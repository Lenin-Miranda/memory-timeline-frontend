Import "./Header.css";

export default function Header() {
  return (
    <div className="header-container">
      <h1>Your Friendship Lane</h1>
    </div>
  );
}

// --- The header of the time-line page, still has to be styled and its sub buttons added ---